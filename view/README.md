### What is the "view" module?
- Renders what is happening in the simulation. 
- The "view" observers the simulation and shows it on the screen.
- Uses "raylib" module.
- From the perspective of the "view" components, the "simulation" module components is essentially a "real-time database" that it reads from. 