### What is the "simulation" module?
- The raw state of the model at each timestep, the objects, and whats happening to them.
- From the perspective of the "simulation", it is as if the "view" does not exist. The simulation does not care about anything else, just itself.